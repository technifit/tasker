{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["WORKOS_API_KEY", "WORKOS_CLIENT_ID"],
      "outputs": ["dist/**"]
    }
  }
}
