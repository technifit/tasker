{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"],
      "env": ["CLOUDFLARE_ANALYTICS_TOKEN", "CODECOV_TOKEN", "SESSION_SECRET", "WORKOS_API_KEY", "WORKOS_CLIENT_ID"]
    }
  }
}
