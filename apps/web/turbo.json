{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"],
      "env": ["SESSION_SECRET", "CODECOV_TOKEN", "WORKOS_API_KEY", "WORKOS_CLIENT_ID"]
    }
  }
}
